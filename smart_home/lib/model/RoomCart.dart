import 'package:flutter/material.dart';

class RoomCart {
  var id;
  var icon;
  var nameRoom;
  var temp;
  var humi;
  var device;

  RoomCart(this.id, this.icon, this.nameRoom,this.temp,this.humi,this.device);

}